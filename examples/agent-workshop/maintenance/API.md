# Release listing

Use showDrafts=true to include draft releases. Omit it to list published releases.

This document intentionally contains one stale parameter name for the workshop.
The implementation uses includeDrafts. Treat that implementation as the source
of truth for this maintenance exercise, unlike the earlier bug-investigation lab.
