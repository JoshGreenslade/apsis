# Working on Apsis

Courses share the contract in `docs/CURRICULUM_AUTHORING.md` and the prefabs in
`prefabs/`. Each course chooses its own teaching style, pace and content structure.

Keep course, lesson and question IDs stable when preserving saved progress.
Run `npm run typecheck`, `npm test` and `npm run build` for architecture changes.
